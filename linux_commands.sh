#!/bin/zsh
#git remote show origin
#git remote set-url origin <url>
#git remote set-url --push origin <url>
#------------------------------------------------------------------------------------------------------------------------------#
sudo su
yum install net-tools
#------------------------------------------------------------------------------------------------------------------------------#

#------------------------------------------------------------------------------------------------------------------------------#
find . -name "<filename>"
find / -name "<filename>"
cp -R <source_location> <destination>
yum install mlocate
updatedb
echo "Hello, $NAME. The current date and time is $(date)"
#------------------------------------------------------------------------------------------------------------------------------#