#!/bin/zsh
#git remote show origin
#git remote set-url origin <url>
#git remote set-url --push origin <url>
#------------------------------------------------------------------------------------------------------------------------------#
# User Accounts
whoami - Find out which user you currently are
id - Find more information about the user(user id, group id, groups)
su - Switch to another user
ssh user@192.168.1.1 - SSH into another machine using a specific user

curl www.world.org -O some-file.txt - Used to download files from the internet
wget www.world.org -O some-file.txt - Used to download files from the internet

ls /etc/*release* - Check the OS version
cat /etc/*release* - View information about the OS version
#------------------------------------------------------------------------------------------------------------------------------#